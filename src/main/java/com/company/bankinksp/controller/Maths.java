package com.company.bankinksp.controller;

import static java.lang.Math.pow;

public class Maths {

    public double getPayment() {
        double creditBalance, interest, limit, payment=0;
        //payment = creditBalance*(interest/(100*12))/(1-pow(1+(interest/(100*12)),-limit));
        return payment;
    }
}
